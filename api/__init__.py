"""
api/ — Capa web de INF_FIN_IA (Fase 7).

Stack: FastAPI + Jinja2 + HTMX + Plotly (server-rendered, sin Node).
La API abre DuckDB en modo read_only para coexistir con el scheduler (único escritor).

NOTA: este paquete es un ESQUELETO. Los endpoints son stubs con la firma y el
contrato de datos definidos; la lógica real está pendiente de implementación.
"""
